name: Build OrangeFox for guacamoleb (Android 15)

on:
  workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 240

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc

      - name: Set up build environment
        run: |
          sudo apt-get update -y
          sudo apt-get install -y git-core gnupg flex bison build-essential zip curl zlib1g-dev libc6-dev-i386 x11proto-core-dev libx11-dev lib32z1-dev libgl1-mesa-dev libxml2-utils xsltproc unzip fontconfig openjdk-11-jdk python3

      - name: Install Repo tool
        run: |
          sudo curl -o /usr/local/bin/repo https://storage.googleapis.com/git-repo-downloads/repo
          sudo chmod a+x /usr/local/bin/repo

      - name: Setup OrangeFox Sync
        run: |
          mkdir OrangeFox_sync
          cd OrangeFox_sync
          git clone https://gitlab.com/OrangeFox/sync.git

      - name: Sync OrangeFox Android 15 Source
        run: |
          cd OrangeFox_sync/sync
          # fox_14.1 branch works for Android 15
          ./orangefox_sync.sh --branch 14.1 --path ~/fox_14.1

      - name: Add Device Tree to OrangeFox
        run: |
          cd ~/fox_14.1
          mkdir -p .repo/local_manifests
          cat > .repo/local_manifests/guacamoleb.xml << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<manifest>
    <remote name="github" fetch="https://github.com/" />
    
    <!-- Your Android 15 Device Tree -->
    <project path="device/oneplus/guacamoleb" name="crdroidandroid/android_device_oneplus_guacamoleb" remote="github" revision="15.0"/>
    <project path="device/oneplus/sm8150-common" name="crdroidandroid/android_device_oneplus_sm8150-common" remote="github" revision="15.0"/>
    <project path="kernel/oneplus/sm8150" name="crdroidandroid/android_kernel_oneplus_sm8150" remote="github" revision="15.0"/>
    <project path="vendor/oneplus/guacamoleb" name="crdroidandroid/proprietary_vendor_oneplus_guacamoleb" remote="github" revision="15.0"/>
    <project path="vendor/oneplus/sm8150-common" name="TheMuppets/proprietary_vendor_oneplus_sm8150-common" remote="github" revision="lineage-22.1"/>
    <project path="hardware/oplus" name="crdroidandroid/android_hardware_oplus" remote="github" revision="15.0"/>
</manifest>
EOF
          repo sync -c -j2 device/oneplus/guacamoleb device/oneplus/sm8150-common kernel/oneplus/sm8150 vendor/oneplus hardware/oplus

      - name: Build OrangeFox Recovery
        run: |
          cd ~/fox_14.1
          source build/envsetup.sh
          export ALLOW_MISSING_DEPENDENCIES=true
          lunch omni_guacamoleb-eng
          make clean
          make recoveryimage -j2

      - name: Upload OrangeFox Recovery
        uses: actions/upload-artifact@v4
        with:
          name: OrangeFox-recovery-guacamoleb
          path: ~/fox_14.1/out/target/product/guacamoleb/recovery.img
          retention-days: 7
